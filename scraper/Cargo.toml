[package]
name = "quakes-scraper"
version = "0.1.0"
authors = ["fdeantoni <fdeantoni@gmail.com>"]
edition = "2018"

[dependencies]
awc = {version = "1.0", features = ["openssl"]}
openssl = "0.10"
scraper = "0.11"
quakes-api = { path = "../api" }
regex = "1"

[dev-dependencies]
actix-rt = "1.0"
